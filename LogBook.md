| Tanggal & Waktu | AI yang digunakan | Prompt / Pertanyaan | Respons AI (Ringkasan) | Implementasi & Refleksi Tim |
| :--- | :--- | :--- | :--- | :--- |
| **Jumat, 14 Nov 2025** | Gemini CLI | "Tolong buatkan struktur dasar HTML dan CSS untuk halaman web yang menampilkan kartu-kartu informasi permainan." | AI memberikan kerangka HTML5 semantik (`header`, `main`, `div` untuk kartu) dan contoh CSS dasar menggunakan Flexbox untuk layout serta variabel warna untuk tema. | Kami berhasil membuat layout awal sesuai arahan. Penggunaan variabel CSS sangat membantu agar tema warna konsisten. Halaman utama dan beberapa kartu statis sudah jadi. |
| **Sabtu, 15 Nov 2025** | Gemini CLI | "Bagaimana cara menampilkan data permainan dari array JavaScript ke dalam kartu-kartu HTML secara dinamis?" | AI memberikan contoh fungsi JavaScript yang melakukan *looping* (menggunakan `forEach`) pada array data, lalu membuat elemen HTML untuk setiap item dan memasukkannya ke DOM. | Kami memindahkan data permainan ke dalam array di `script.js`. Fungsi `renderGames()` berhasil dibuat dan sekarang semua data game muncul otomatis saat halaman dimuat. |
| **Minggu, 16 Nov 2025** | Gemini CLI | "Saya punya form filter. Bagaimana cara membuat fungsi agar daftar permainan bisa ter-filter secara langsung saat pengguna memilih lokasi atau mengisi jumlah pemain?" | AI menjelaskan penggunaan *event listener* (`change` dan `input`) pada form, dan cara menggunakan fungsi `array.filter()` untuk menyaring data berdasarkan kondisi yang sesuai dengan input pengguna. | Fungsi `filterGames()` berhasil diimplementasikan. Kami belajar menggabungkan beberapa kondisi filter. Sekarang aplikasi terasa lebih interaktif karena hasilnya langsung berubah. |
| **Senin, 17 Nov 2025** | Gemini CLI | "Bagaimana cara membuat jendela pop-up (modal) untuk menampilkan detail 'Cara Bermain' tanpa pindah halaman?" | AI memberikan contoh kode untuk membuat modal dengan HTML/CSS (awalnya `display: none`) dan fungsi JavaScript untuk mengubah properti `display` menjadi `block` saat tombol diklik. | Modal berhasil ditambahkan. Kami juga membuat fungsi `openModal()` dan `closeModal()` untuk mengelolanya. Tampilan detail permainan sekarang jauh lebih rapi dan informatif. |
| **Selasa, 18 Nov 2025** | Gemini CLI | "Kode JavaScript saya mulai berantakan karena data dan logika tercampur. Apa ada cara untuk merapikannya?" | AI menyarankan dua hal: 1) Memisahkan data `games` ke dalam file `games.json` dan mengambilnya menggunakan `fetch()`. 2) Membungkus semua kode ke dalam IIFE (Immediately Invoked Function Expression) agar tidak mencemari *global scope*. | Kami mengikuti saran AI. Data sekarang ada di `games.json` dan `script.js` jadi lebih bersih karena hanya berisi logika. Kode juga lebih aman dengan IIFE. Proyek jadi lebih terstruktur dan siap untuk pengembangan fitur selanjutnya. |